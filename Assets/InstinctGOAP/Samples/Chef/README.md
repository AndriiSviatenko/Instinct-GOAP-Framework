# Chef sample

Open `Chef_Kitchen_Demo.unity` and enter Play Mode.

This is the recommended first sample. It demonstrates the Command API, multi-step cooking plans, world-state synchronization, dynamic costs, goal relevance, action completion, policy stickiness, and domain validation.

The scene uses TextMesh Pro for labels and is render-pipeline agnostic.

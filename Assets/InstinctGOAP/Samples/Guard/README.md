# Guard sample

Open `Guard_Patrol_Demo.unity` and enter Play Mode.

This sample demonstrates a production-style Command API integration: sensing, a blackboard, typed facts, action translation, goal preemption, policy stickiness, patrol continuation, and live graph visualization.

The scene uses TextMesh Pro for labels and is render-pipeline agnostic.
